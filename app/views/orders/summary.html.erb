<%= render partial: 'order_header', locals: { title: "Step 5: Confirm Order" , progress: 84 } %>

<%= bootstrap_form_for work_order, url: wizard_path, method: :put  do |f| %>

  <%= f.hidden_field :id %>

  <%= render 'work_orders' %>
  <%= render 'sets' %>
  <%= render 'products' %>
  <%= render 'proposals' %>

  <%= render 'buttons', work_order: work_order, f: f %>

<% end %>