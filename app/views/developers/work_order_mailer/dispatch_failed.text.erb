Dispatch failed for Work Order <%= @work_order.id %> (owned by <%= @work_order.work_plan.owner_email %>)
========================================================================

There was an error while trying to process Que::Job <%= @que_job.que_attrs[:id] %>. This Que::Job has failed <%= @que_job.que_attrs[:error_count] %> times before. It will be attempted a maximum of <%= @que_job.class.maximum_retry_count %> times.

The following exception was raised:

<%= @exception.message %>

<%= @exception.backtrace %>