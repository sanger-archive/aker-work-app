<%= render partial: 'plan_header' %>

<%= bootstrap_form_for work_plan, url: wizard_path, method: :put  do |f| %>

  <% enabled = work_plan.in_construction? %>

  <%= react_component('ProductDescription', { data: get_current_catalogues_with_products, num_samples: work_plan.original_set_size, work_plan_id: work_plan.id, productId: work_plan.product_id, enabled: enabled }) %>

  <%= render 'buttons', work_plan: work_plan, f: f %>

<% end %>
