<%= render partial: 'plan_header' %>

<%= bootstrap_form_for view_model.work_plan, url: wizard_path, method: :put  do |f| %>

  <%= react_component('ProductDescription', { data: view_model.current_catalogues_with_products, num_samples: view_model.number_of_samples, work_plan_id: view_model.work_plan.id, productId: view_model.work_plan_product_id, enabled: view_model.form_enabled? }) %>

  <%= render 'buttons', work_plan: work_plan, f: f %>

<% end %>
